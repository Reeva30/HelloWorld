name: Build and Test

on: [push, pull_request]

env:
  PROJECT_FILE_PATH: HelloWorld.vcxproj
  BUILD_CONFIGURATION: Debug|x64 
  DEVENV_PATH: C:\Program Files\Microsoft Visual Studio\2022\Enterprise\Common7\IDE\devenv.exe
  QT_DIR: C:\Qt\5.15.2\msvc2019_64

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Chocolatey
      run: |
        Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

    - name: Set up Qt
      run: |
        choco install qt5-default --params "/InstallDir:C:\Qt"
        echo "C:\Qt\5.15.2\msvc2019_64\bin" >> $env:GITHUB_PATH
        refreshenv

    - name: Set up MSVC environment
      shell: cmd
      run: |
        call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\Common7\Tools\VsDevCmd.bat"

    - name: Build with Visual Studio
      shell: cmd
      run: |
        set INCLUDE=%INCLUDE%;C:\Qt\5.15.2\msvc2019_64\include
        set LIB=%LIB%;C:\Qt\5.15.2\msvc2019_64\lib
        "%DEVENV_PATH%" "%PROJECT_FILE_PATH%" /Build "%BUILD_CONFIGURATION%"
